PROJECT WORKFLOW:
USER:

* Bus search page (search buses with respect to src, destination and date)


* Then bus list loaded on the same page with price, timings, name and available seats.

*On clicking a specific bus, user must be able to view full details of the bus along with its layout.

* To select a seat, a user must login, (if not logged in re - directed to login page and after login re - directed to same bus layout page).

* The user must block seats, add passenger details like (name, age and gender)

*User directed to the Transaction page.

*Dummy transaction page that collects card details or upi details 

*After which transaction successful and booking successful message must be prompted on the webpage

*The ticket must be downloadable at the user's device and must be sent to their mail from the admin mail.

*The journey details must be automatically added to user's booking page