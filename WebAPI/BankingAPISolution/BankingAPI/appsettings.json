{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "JWT": {
    "Key": "BankingAPI_JWT_SECRET_KEY_2026_SUPER_LONG_32_PLUS_CHARS",
    "Issuer": "G3 Server",
    "DurationInMinutes": 60
  },
  "ConnectionStrings": {
    // "Default": "Host=localhost;Port=5433;Database=BankingAPI;Username=postgres;Password =secret; Include Error Detail=true" => docker migration (host to container)
    // "Default": "Host=pg;Port=5432;Database=BankingAPI;Username=postgres;Password=secret; Include Error Detail=true" (container to container)
    "Default": "Host=localhost;Port=5432;Database=BankingAPI;Username=postgres;Password=secret; Include Error Detail=true"
  },
  "Serilog": {
    "Using": [ "Serilog.Sinks.Console", "Serilog.Sinks.File" ],
    "MinimumLevel": "Information",
    "WriteTo": [
      { "Name": "Console" },
      {
        "Name": "File",
        "Args": {
          "path": "logs/MyAppLog-.txt",
          "rollingInterval": "Day"
        }
      }
    ],
    "Enrich": [ "FromLogContext", "WithMachineName", "WithThreadId" ],
    "Properties": {
      "Application": "BankingAPI"
    }
  },
  "AllowedHosts": "*"
}

